# TODO

## Open

- [ ] Follow-up polish: apply the rotating-spinner treatment (built for consultation-chat, spec 0024) to `debate-thread.css`'s identical-looking 3-dot `.typing-indicator` — 4 spots (`debate-thread.html`: opening-statement thinking, the generic "Generating opening statement…" fallback, the argument bubble's thinking state, and the verdict's thinking state). **Re-scoped after checking the actual markup**: unlike consultation-chat, none of these have a multi-step label that changes while visible — each is a single static label ("X is thinking…") shown only until the first real streamed token arrives, at which point the dots/label are replaced entirely by the growing `streamingText()` (specs 0020/0021), never flipping between multiple strings. So only the spinner swap applies here — the flip-label and JS-measured smooth-resize logic from spec 0024 have nothing to apply to (continuously-growing streamed text would fight a measured-width transition, not benefit from one). Confirmed with the user; left for later, not yet started.

- [ ] Cosmetic, very minor: during the opening-statement and verdict streaming-to-final swap (spec 0021), the small avatar+label line (not the text itself, which stays fully visible throughout) blinks out for a single ~45-50ms frame right before the final block mounts. Found via frame-by-frame re-verification of the (separate, fixed) dots-revert bug — reproducible on 2 fresh debates, but sub-perceptual at normal viewing speed. Not fixed — logged for awareness; likely not worth a dedicated pass given its size, revisit if it turns out to be more noticeable than the frame capture suggests.

## Done

- [x] App-wide motion pass (ADR 0009/spec 0026): everything in the app rendered/changed state instantly before this — flagged by the user after testing spec 0020's live token streaming made the rest of the UI feel abrupt by comparison. Scoped directly with the user rather than assumed: interactive hover/focus/active/disabled states, route transitions, and mount/unmount transitions for existing `@if`-gated messages are in scope; scroll-behavior polish was explicitly dropped — verified by grep that the only two scroll interactions anywhere in the app (`debate-thread.ts`/`consultation-chat.ts`'s auto-follow-to-bottom effects) reassign `scrollTop` on every render tick during streaming, so `scroll-behavior: smooth` there would fight itself (jank, not glide), and nothing else in the app has a scroll interaction to apply it to at all. Two Angular mechanisms used for the first time in this codebase, both verified against current docs rather than assumed, both needing **zero new dependencies**: `withViewTransitions()` (native router feature wrapping the browser's View Transitions API, progressive enhancement — no fallback code needed) for route cross-fades, and `animate.enter`/`animate.leave` template bindings (the documented replacement for the now-deprecated `@angular/animations` package) for message mount/unmount. New shared `--transition-fast`/`--transition-base` tokens in `styles.css` (mirroring the existing `--panel-shadow` "one value app-wide" convention) applied only to properties that already changed state instantly before (navbar link/button hover, disabled-button opacity across all 4 forms, debate-thread's OPEN/JUDGED and Minimal/Detail toggle buttons' active-state swap) — no new hover states invented. Also fixed a real gap found while scoping: the existing global `prefers-reduced-motion` rule only zeroed `animation-duration`, not `transition-duration`, and didn't reach `::view-transition-*` pseudo-elements at all — fixed once, globally. Verified in a real browser with `getComputedStyle`/`getAnimations()`, not a glance: navbar hover confirmed a real non-zero `transition-duration` mid-hover; the debate-thread toggle buttons and "Start debate" button confirmed running transitions right after interaction; login/register/consultation-chat's error messages confirmed a full mount→unmount→remount cycle via `getAnimations()` (opacity actually stepping through intermediate values, not a single-frame jump); a real route navigation confirmed genuine native `::view-transition-old/new(root)` pseudo-animations firing (Chrome 145); `prefers-reduced-motion: reduce` emulation confirmed every new transition/animation collapses to near-instant (`0.000001s`, deliberately not a literal `0`, which can suppress `transitionend`/`animationend` events); all pre-existing animations this pass didn't touch (consultation-chat's thinking spinner, debate-thread's typing-bounce indicator and live-badge pulse) confirmed still running correctly across real consultation/debate runs. 26 Angular tests pass, zero console errors. See [docs/adr/0009-app-wide-motion-system.md](docs/adr/0009-app-wide-motion-system.md) and [docs/specs/0026-app-wide-motion-pass.md](docs/specs/0026-app-wide-motion-pass.md). — 2026-08-16

- [ ] Found during spec 0026's QA, out of scope for that spec: a real consultation session's `ready_to_finalize` never went true across several thorough, explicit answers in two separate consultation flows (loan_approval and research_debate case types) during verification. LLM-judged, not yet root-caused — worth a dedicated look (is the finalize-readiness prompt/threshold too strict, or is something else gating it) before assuming it's just conversational-length variance (the existing UX note logged under spec 0023 already observed the consultant can take several back-and-forth turns before finalizing even when told plainly to).

- [ ] Ops/cleanup, not a bug: the dev DB has accumulated a growing pile of leftover throwaway QA test users/cases/debates from prior Canary QA sessions (`canaryqa*`, `browsertest*`, `consulttest001`, etc. — first flagged during spec 0025's QA, grown further during spec 0026's, which also deleted one QA-owned debate's arguments/verdict to force a live re-run for a test — confirmed harmless, real user `tracer`'s data untouched). Worth a cleanup pass at some point; not urgent, no real data at risk so far.

## Done

- [x] Humanized raw backend status/case-type strings shown verbatim in the UI (spec 0025): the last item from the original 9-item feedback batch. Two independent root causes needed two different fixes, discussed with the user before implementing rather than assumed: (1) `Debate.status` (e.g. "NO_CONSENSUS", "ARGUING") already had real human labels defined server-side via Django's `Status.TextChoices`, just never exposed — `DebateSerializer` only listed the raw `status` field. Fixed by adding a `status_display` field (`source="get_status_display"`) alongside the unchanged raw `status`, so `debate-thread.html`'s `d.status === 'OPEN'` branching logic keeps working exactly as before while only the *displayed* text (`d.status_display`) changes. (2) Case-type slugs (e.g. "loan_approval", "research_debate") have no backend label at all — `CaseTypeConfig.type` is a free, admin-extensible `CharField`, confirmed by querying the real DB directly (only 2 rows exist today). Considered adding a backend `display_name` field vs. a frontend-only generic transform; chose the frontend transform (lower cost, both current slugs read cleanly under simple title-casing) after discussing the tradeoff with the user. New `HumanizeSlugPipe` (scaffolded via `ng generate pipe`, per the generator-first rule), applied to `debate-thread.html`'s case-type heading and `consultation-chat.html`'s case-type `<select>` options. Verified: direct curl against a real `NO_CONSENSUS` debate confirmed both `status: "NO_CONSENSUS"` and `status_display: "No consensus"` in the same response; 5 new pipe unit tests plus the full 26-test Angular suite pass; a real browser (debate 2, a genuine `NO_CONSENSUS`/`research_debate` case) confirmed the status line reads "No consensus", the case-type heading reads "Research debate", and — the specific regression risk — no active-debate/in-progress UI wrongly appeared, confirming the raw-status branching logic is unaffected; the consultation-chat dropdown confirmed both options read "Loan approval"/"Research debate". Zero console errors, zero bugs found. See [docs/specs/0025-humanize-status-and-case-type-labels.md](docs/specs/0025-humanize-status-and-case-type-labels.md). — 2026-08-16

- [x] Redesigned consultation-chat's "thinking" indicator (spec 0024): the 3-dot bouncing indicator plus an instantly-snapping step label (spec 0023) felt "overloading" to the user once the label started changing mid-turn. Replaced with a single continuously-rotating circular spinner (a CSS ring with a transparent top edge, kept structurally outside Angular's `@switch` so its animation never restarts when the label changes) plus a literal 3D flip animation on the label text (gotten "for free" by putting the label *inside* the `@switch`, since Angular genuinely recreates that element on every case change, so a plain CSS `animation` just replays automatically) plus a smoothly-animated bubble width. Checked before assuming an approach: the obvious modern CSS-only way to animate toward an unknown/`fit-content` width (`interpolate-size`/`calc-size()`) only works in Chrome/Edge today, not Firefox/Safari — used the reliable cross-browser alternative instead (measure the new label's actual width via `getBoundingClientRect()` in an `afterRenderEffect`, same pattern already used for auto-scroll in this file, and animate a plain `transition: width` to that explicit px value). Verified in a real browser with actual DOM/CSS measurements, not a glance: spinner rotation confirmed monotonic across label changes (2457.9° cumulative over a 6.142s window ≈ 6.83 cycles, matching the declared 0.9s/cycle rate almost exactly, no reset/backward jump at the label-change boundary); the flip animation confirmed actually running (`getAnimations()` showing `currentTime` climbing 0→~300ms) on a genuinely new DOM element each time, not a static declaration; the bubble width confirmed transitioning through multiple real intermediate values (not a single-frame jump) on both a widening and a narrowing change. **A real bug was caught by the user immediately after this first pass**: longer labels ("Double-checking…", "Revising…") were wrapping onto two lines instead of the bubble expanding to fit them — `.step-label` had no `white-space: nowrap`, so new (longer) text wrapped to fit the *current* (stale) width instead of overflowing, and the measurement effect then read the wrapped box's already-constrained width, never letting the bubble actually grow. Fixed by adding `white-space: nowrap` (forces the true single-line width to always be measurable) and, per the same feedback, made the label bold (`font-weight: 600`). Re-verified in a real browser across 3 turns: all three labels confirmed single-line via `getClientRects().length === 1`, bubble width matched the label's natural width to sub-pixel precision, `fontWeight` confirmed `600` on every sample. **A second real bug was caught right after that fix**: the spinner+label content had lopsided spacing — a large gap before the spinner, none after the text. Root cause: both the bubble and the inner content wrapper (`.thinking-inner`) each had their own padding, but the measurement effect only ever read the inner wrapper's already-padded width and applied that number as the *bubble's* total border-box width — so, under the app's global `box-sizing: border-box`, the bubble had to squeeze its own 24px of padding out of that same number, clipping the right side (via `overflow: hidden`) while the left padding (which always reserves its own space) stayed visible. Fixed by removing `.thinking-inner`'s padding entirely (only the bubble's own padding applies now, same as every other message bubble in this chat) and reading the bubble's actual computed padding via `getComputedStyle` to add to the inner content's now-unpadded natural width. Re-verified in a real browser: left gap and right gap both measured exactly 12px for all three labels. Debate-thread's identical-looking dots explicitly deferred as a separate follow-up (logged above), per the user's own scoping choice. See [docs/specs/0024-consultation-thinking-indicator-redesign.md](docs/specs/0024-consultation-thinking-indicator-redesign.md). — 2026-08-16

- [x] Consultant reflection (draft → critique → conditional revise) + a live step indicator, backend + frontend (spec 0023/ADR 0008): gives the consultant a real self-check step, addressing concrete evidence it previously parroted back an evident typo three times without ever questioning it. `orchestrator/app/consultations/graphs.py`'s single-node graph became three: `draft` (unchanged) → `critique` (new, judges the draft against the transcript) → conditional `revise` (new, only runs if critique flags a concern, reusing `ConsultantTurnOutput`'s shape rather than inventing a second one) — the codebase's first genuinely multi-node LangGraph graph with a conditional branch. Bundled in a live step indicator so reflection's added latency doesn't ship with zero visibility: a new `GET /api/consultations/{id}/stream` SSE endpoint (the codebase's first, using FastAPI's native SSE support, no new dependency) relays `{"step": "draft"|"critique"|"revise"}` from a new `consultation:{id}:stream` Redis channel — additive only, `POST /messages` stays the unchanged, sole source of truth for the actual reply. **Three real integration bugs found and fixed during implementation/verification** — this being the first-ever conditional edge through Temporal's LangGraph plugin and the first-ever FastAPI SSE endpoint in this codebase, exactly the kind of untested integration surface the spec's verification plan was written to catch: (1) the routing function was a plain sync function, and Temporal's workflow sandbox forbids the background-thread fallback LangChain uses for sync callables — fixed by making it `async def`; (2) the SSE endpoint was built as a function that returns `EventSourceResponse(a_generator())`, but FastAPI requires the route handler itself to be the generator — fixed to match FastAPI's documented pattern exactly; (3) a previously-working 404 (nonexistent/not-owned session) silently became an empty 200 once the endpoint became a generator, since an exception raised inside a streaming generator body doesn't convert to a normal error response — fixed by moving the ownership check into a `Depends(...)`, which FastAPI resolves before the generator ever runs. Verified thoroughly against the real stack: both graph branches confirmed (3 real consultation turns via curl all triggered revise, with the *revised* text — not the original draft — confirmed persisted in Postgres; the clean-pass/no-revise branch confirmed via a deterministic isolated test plus a real browser turn that legitimately skipped revision); the SSE ordering requirement (subscribe before POST) confirmed real by deliberately getting it wrong once and observing the missed event; disconnect cleanup confirmed working with no hand-built race needed (unlike spec 0022), matching Starlette's documented internal behavior for streaming responses; a real browser across 4 consultation turns showed clean "Thinking…"→"Double-checking…"→(sometimes)"Revising…"→reply label transitions with no stuck states or flicker, zero console errors, and approve/finalize unaffected. One calibration observation logged, not a bug: critique flagged a concern more often than not across the turns observed — worth revisiting the prompt's threshold in a future pass. 21 Angular tests pass (1 new). See [docs/adr/0008-consultant-draft-critique-revise.md](docs/adr/0008-consultant-draft-critique-revise.md) and [docs/specs/0023-consultant-reflection-and-step-indicator.md](docs/specs/0023-consultant-reflection-and-step-indicator.md). — 2026-08-15

- [x] Fixed `WS /api/debates/{id}/stream`'s disconnect-leak (spec 0022): the handler could only ever discover a client disconnect when `send_text()` next raised — and since nothing is published again to a finished debate's channel, that never happened, leaking the Redis subscription + asyncio task per finished debate indefinitely (confirmed live on a real session, 2026-07-19: 6 finished debates' channels still subscribed 10+ minutes later with zero errors logged). Fixed by racing the existing Redis-forward loop against a new disconnect-watcher loop (`websocket.receive_text()`, which raises `WebSocketDisconnect` the instant the client closes — verified against current Starlette/FastAPI docs, not assumed) via `asyncio.wait(..., return_when=asyncio.FIRST_COMPLETED)`, cancelling whichever side didn't finish first. Verified against the real stack, both a raw WS client and a real browser across 5 scenarios: connecting to an already-terminal debate then disconnecting (the exact leak scenario) now cleans up within ~1s, where before it never happened at all; a full debate run confirmed zero regression to normal event delivery; a real browser navigating away mid-run cleaned up in ~20ms. **One genuinely useful, non-bug finding along the way**: a real browser that stays on the page through an entire debate run already closes its own connection proactively the instant it detects the debate finished (pre-existing `debate-thread.ts` logic from spec 0014) — 24 seconds before it was ever navigated away in that test. This fix's practical value is specifically for the cases that graceful path can't cover: an abrupt tab crash/force-quit, a network drop, or a missed final event — exactly what the raw-client tests (which have no such logic of their own) directly exercise. No frontend changes needed. See [docs/specs/0022-fix-ws-stream-disconnect-leak.md](docs/specs/0022-fix-ws-stream-disconnect-leak.md). — 2026-08-15

- [x] Real token-by-token streaming for the judge's opening statement and verdict, backend + frontend (spec 0021/ADR 0007) — completes ADR 0007's sequencing, extending spec 0020's pattern (below) to the two remaining graphs. Opening statement needed no split at all: its schema (`JudgeOpeningOutput`, now deleted) was 100% prose (`opening_statement` only), so it just drops `with_structured_output` and streams directly — confirmed via the WS event timing that only one LLM call happens now, not two. Verdict split like arguments did: only `reasoning` is actually rendered live (`closing_summary` is persisted but never displayed anywhere in the UI, confirmed by grep), so it streams while `decision`/`confidence`/`closing_summary`/`cited_argument_ids` come via a fast follow-up `ClosingJudgment` call fed the streamed reasoning back as fixed context. Applied spec 0020's lesson up front this time — the closing prompt explicitly forbids JSON/verbatim-echoing from the first draft — and it worked: no repeat of the round-2 degeneration bug, confirmed clean on a fresh debate with an obvious "deny" outcome (the same kind of low-novelty case that triggered it last time). **A different real bug was found and fixed within this pass**: right as the opening statement finished streaming, the visible text briefly (~100-150ms) reverted to a "Generating opening statement…" dots placeholder before the final block popped in — caught via 20fps frame extraction. Root cause: the WS handler cleared the live streamed text synchronously the instant the completion event arrived, but the real data refetch is async, so in that gap an older fallback (built for a different scenario in spec 0017/0019) took over and showed unconditional dots with no awareness of the leftover streamed text. Fixed by no longer clearing the streamed text on completion (it persists until the next turn naturally resets it) and teaching that fallback to show it instead of dots when present. Re-verified with continuous, gap-free ~20-22fps frame polling across 2 fresh debates for both the opening and verdict transitions — real text visible throughout, never reverts, seamless swap to final content both times. One new, separate, very minor cosmetic finding logged above (avatar/label line blinking for a single sub-perceptual frame). 20 Angular tests pass, zero console errors. See [docs/adr/0007-two-phase-turn-generation-for-token-streaming.md](docs/adr/0007-two-phase-turn-generation-for-token-streaming.md) and [docs/specs/0021-opening-and-verdict-token-streaming.md](docs/specs/0021-opening-and-verdict-token-streaming.md). — 2026-08-06

- [x] Real token-by-token streaming for arguments, backend + frontend (spec 0020/ADR 0007): closes the last real gap in decision 12/spec 0001's original streaming ask, after tracing the earlier belief that Temporal itself was the blocker back to its actual root cause — Temporal was never the issue (an Activity is just Python; it can publish to Redis mid-execution like any other side effect), it was always the *combination* of streaming with `.with_structured_output()` (re-confirmed against ADR 0006, not re-guessed). Fix: split argument generation into a plain-streamed call for `content` (published token-by-token as a new `turn_token` WS event, straight from the LangGraph node itself — a new responsibility for `graphs.py`, not `activities.py`) followed by a fast structured `ArgumentJudgment` call (position/confidence/citation only) fed the streamed content back as fixed context, so the persisted row can never drift from what streamed. A new `turn_token_reset` event resolves the `retry` event ADR 0006 explicitly deferred, for exactly this reason. Deliberately paired backend+frontend in one spec — confirmed during design discussion that shipping the backend alone would break the frontend, since its WS handler treated anything that wasn't `turn_started` as "turn complete, re-fetch," which would have misfired on every token. Verified against the real stack: a direct WS test client showed genuine incremental streaming (75 chunks for one argument, ~11ms apart) with correct `turn_token_reset` behavior and a full debate finishing in ~13.5s (well under the 60s Activity timeout); a real browser confirmed word-by-word visible growth (frame-evidenced), a clean swap to the final bubble with no flash, and — the specific regression this pairing was meant to prevent — zero extra `GET` refetches during the token-streaming window. **A real bug was found and fixed within this pass**: round 2 (both participants) persisted `content` as a raw stringified-JSON blob instead of prose — losing `with_structured_output()`'s implicit shape constraint let the model degenerate into echoing the prior-arguments JSON it was handed in the prompt once it had nothing new to add (both already at `reject`/0.9, a fairly clear-cut case). Fixed with an explicit prompt instruction (plain prose, no JSON, don't repeat prior-arguments verbatim) and re-verified clean with a fresh debate, confirmed both in Postgres and via real-browser DOM inspection. Judge opening statement and verdict remain on the old one-shot call, logged as a follow-up above. 20 Angular tests pass (1 new), zero console errors. See [docs/adr/0007-two-phase-turn-generation-for-token-streaming.md](docs/adr/0007-two-phase-turn-generation-for-token-streaming.md) and [docs/specs/0020-argument-token-streaming.md](docs/specs/0020-argument-token-streaming.md). — 2026-08-06

- [x] Fixed debate-thread's inverted left/right agent sides and bottom-aligned avatars (a real bug in spec 0019's CSS, caught immediately by the user after using it — not a preference). Root cause: `.bubble-row` combined `flex-direction: row-reverse` with a universal `justify-content: flex-end` and no `.left` override — that combination packs content toward row-reverse's main-*end*, which is the **left** edge, the opposite of what was intended, so the agent meant to render on the right was rendering on the left and vice versa. `align-items: flex-end` (bottom) was also wrong for a multi-line bubble — the avatar should align with the header/name line at the top. Fixed by replacing the row-reverse trick entirely with explicit `order` values on the avatar (unambiguous, no directional footgun) and changing to `align-items: flex-start`. Verified with real `getBoundingClientRect()` measurements across 2 rounds: both agents confirmed on consistent, correct, opposite sides; avatar top edge exactly matches bubble top edge in all 4 measured instances; avatar sits just outside the bubble's outer edge with no overlap. 19 Angular tests pass (unaffected, CSS-only), zero console errors. See [docs/specs/0019-debate-thread-live-turn-indicator-and-polish.md](docs/specs/0019-debate-thread-live-turn-indicator-and-polish.md) (amended, not a new spec — a straightforward correction to that milestone's own CSS). — 2026-07-30

- [x] Live "who's generating" turn indicators + polish (specs 0018 backend/0019 frontend): traced a real user complaint ("very bad UI... no interactivity of who is generating") back to the original requirement (decision 12/PRD/spec 0001's mock wanted token-by-token streaming with a "currently generating" indicator). ADR 0006 had correctly ruled out *token content* (the LLM's structured-output call returns one atomic chunk, verified empirically) — but "which agent/judge is active right now" doesn't need token content at all, and that part was never generalized past the one-off opening-statement indicator (spec 0017). Backend (0018): a new `turn_started` event, published the instant a turn's LLM call begins (not completes) at 3 points in `workflows.py` — judge opening statement, each participant's argument, judge verdict — carrying the agent's name directly so the frontend never needs a lookup. Frontend (0019): `DebateStream` now actually parses and forwards WS messages (previously discarded); a live "{agent} is thinking…" indicator appears in the correct position/side per stage, replaced by real content the instant it lands; plus, bundled in for the same "who's doing what" problem: avatar icons beside every bubble/judge block, timestamps per argument, and two new distinct per-agent bubble color tokens (`--agent-a`/`--agent-b`, blue/rose). **Found and fixed a real bug during verification**: the opening-statement slot went completely blank for ~2s once the first round's `turn_started` arrived, because (a) `persist_opening_statement` never published its own completion event — the frontend only learned via whatever unrelated event came next — and (b) the template's `@else if (generatingTurn(); as gt)` branch was "claimed" by any stage, not just the matching one, blocking fallthrough to the fallback. Fixed both (a new `opening_statement_complete` publish; a `openingGeneratingTurn` computed that's null for non-matching stages) and re-verified clean via continuous DOM polling across 2 full debate runs — no blank frame at ~150-200ms resolution either time. 19 Angular tests pass, zero console errors. See [docs/specs/0018-debate-turn-started-events-backend.md](docs/specs/0018-debate-turn-started-events-backend.md) and [docs/specs/0019-debate-thread-live-turn-indicator-and-polish.md](docs/specs/0019-debate-thread-live-turn-indicator-and-polish.md). — 2026-07-30

- [x] Debate-thread opening statement + loading state (spec 0017): fixed the "looks frozen after clicking Start debate" bug — root-caused against a real run (Case #23/debate 23): the WS/streaming pipeline itself was fine (4 events pushed within single-digit ms of their DB writes), but the workflow's judge-authored opening statement took 21 seconds on its own before the first `Argument` existed, and `Debate.opening_statement` — already fetched — had never been rendered anywhere (a gap spec 0016 flagged as "pre-existing, not touched" and under-weighted). Now: the opening statement renders as the thread's first entry once ready ("Opening Statement · {judge}"), and a "Generating opening statement…" typing-dots indicator (reusing the exact pattern from `consultation-chat.css`) fills the gap before it exists, replacing a silent static "No arguments yet." **Found and fixed a regression in the same pass**: the first implementation gated the loading indicator on `isActive()`, which deliberately includes `OPEN` for an unrelated reason (starting the WS stream early) — this made the indicator fire before "Start debate" was even clicked, caught by real-browser verification and fixed with `&& d.status !== 'OPEN'`, then re-verified clean (explicit DOM assertions: no loading-indicator element mounted pre-start, correct "No arguments yet." text). Full debate replay (opening statement → rounds → verdict) confirmed working after the fix, zero console errors. 18 Angular tests pass. See [docs/specs/0017-debate-thread-opening-statement-and-loading-state.md](docs/specs/0017-debate-thread-opening-statement-and-loading-state.md). — 2026-07-30

- [x] Debate-thread center-stage restyle (spec 0016): replaced the scatter-plot node graph + separate click-to-read side panel with one scrolling chat thread — bubbles left/right by agent (stable first-seen order), full argument text inline, round-divider labels, an inline "↩ Responds to X, round N" citation line for rebuttals, and the verdict as the thread's final entry ("Verdict · {judge}"). Prompted by stepping back mid-session to question whether the app/UI had been accumulating complexity without a clear payoff — restyle only, same `content`/`position`/`confidence`/`respondsToId` data already fetched, no backend/schema change. This is a genuine deletion, not just an addition: removed spec 0015's lane-offset math, agent-initial legend, node-position/color CSS animations, the split-panel/verdict-scroll layout, and the `userHasSelected` live-follow-selection logic — **including its logged race-condition bug, resolved by removing the mechanism it depended on, not by fixing it** (no more "selected argument" state left to race on). Mode toggle kept exactly as it renders today per explicit instruction, now a no-op (nothing left for it to open/close). Verified in a real browser across 2 full debates (one unanimous, one a genuine mind-changing case to actually exercise the rebuttal-citation line): bubbles consistently left/right per agent every round (DOM-measured), round grouping correct, auto-scroll numerically confirmed pinned to bottom (`scrollTop + clientHeight == scrollHeight`), rebuttal citations cross-checked directly against `Argument.responds_to` in Postgres (not a UI fluke), verdict block correct, mode toggle still highlights on click, WS still live (confirmed via orchestrator logs), zero console errors. 18 Angular tests pass. See [docs/specs/0016-debate-thread-chat-restyle.md](docs/specs/0016-debate-thread-chat-restyle.md). — 2026-07-30

- [x] Django superuser created (ops action, no code change) — `adadmin001@mailinator.com`, verified working end to end via a real login POST (redirected to the actual admin dashboard, not back to the login form, confirming both the account and password work), not just that the CLI command reported success. — 2026-07-19

- [x] Debate-thread visualization overhaul (spec 0015 — spec A of a 3-spec batch from a round of user feedback on the debate view; specs B "enum humanization" and C "consultant reflection" are separate, not yet started): 6 fixes to the same screen, all verified root-caused against real code/data first (a live user session — consultation 16 → debate 17 — was dug into directly to ground each finding before the spec was written). **Node overlap**: same-round/same-position arguments used to render at the exact same (x,y) — `get_leaning()` never factored in which agent made the argument. Fixed with a stable per-agent "lane" offset (first-seen order, ±multiples of 36px around each round's line) — doubles as a consistent per-agent visual identity across rounds. **Monogram bug**: was the *last* letter of the agent's name (`.slice(-1)` — "Pragmatist" showed "t"), now the first letter, plus a real legend mapping initial → full name. **Judge**: was completely absent from the UI despite already being in the API response — now shown in the header meta row and the verdict card header ("Verdict · {judge name}"). **Live-follow**: the reading panel used to get stuck on "No arguments yet." forever unless the user clicked a node themselves, even as the graph filled up live — now auto-advances to the newest argument until the user manually selects one, after which their choice is respected exactly as before. **Animation**: node position/color changes were instant jumps — now CSS-transitioned (~0.4s glide) with a fade/grow-in for new nodes, respecting `prefers-reduced-motion`. **Split verdict panel**: the verdict card used to be appended in the same scroll flow as the argument text, so its position shifted with argument length — now a separate, fixed-height region (verified via `getBoundingClientRect()`: identical `{top, bottom, height}` whether a short or a 700-character argument was selected), with only the reasoning paragraph itself scrolling internally if long. Verified in a real browser across 3 full debate runs: 5 of 6 fixes confirmed clean with concrete measurements/screenshots; the 6th (live-follow) surfaced a real, narrow, timing-dependent race condition (logged separately above, not fixed in this pass — treated the same as the earlier WS-leak bug). 18 Angular tests still pass. See [docs/specs/0015-debate-thread-visualization-overhaul.md](docs/specs/0015-debate-thread-visualization-overhaul.md). — 2026-07-19

- [x] Debate-streaming frontend: wired `debate-thread` to the real `WS /api/debates/{id}/stream` (spec 0013/ADR 0006), replacing the 4s poll with instant push. New `DebateStream` service opens the WebSocket with the JWT on the subprotocol field; any message triggers the same full `loadDebate()` refresh a poll tick used to (both event types are just "something changed, go re-fetch," not payload carriers). Removed the dead `isStreaming` field/CSS (never set `true` under complete-event push — ADR 0006 flagged this explicitly). Polling kept as a visible (`console.warn`-logged), non-silent fallback for an unexpected WS drop, not deleted outright. Verified in a real browser across 4 real debates: WS opens within ~300ms of page load, a full 6-argument debate completed in ~16.5s with updates rendering far tighter than the old 4s window, zero console errors, no lingering connection after a debate finished (confirmed via reload). 18 Angular tests pass (2 new). See [docs/specs/0014-debate-streaming-frontend.md](docs/specs/0014-debate-streaming-frontend.md). — 2026-07-19
- [x] Redis pub/sub + WebSocket live debate streaming, backend only (decision 12/13a): replaces debate-thread's 4s polling with instant push. Scoped to complete-event push (argument/status events pushed the moment they're ready) after empirically proving true token-by-token argument streaming doesn't compose with the current structured-output LLM call — tested directly against the real model: `.with_structured_output().astream()` yields exactly one atomic chunk, while plain `.astream()` yields real incremental chunks, confirming the limit is the structured-output combination specifically, not streaming in general (see ADR 0006). New: `redis==8.0.1` client, `redis_url` config (fail-fast), publish calls at existing Activity boundaries (no workflow/graph changes), and the first WebSocket endpoint in the app (`WS /api/debates/{id}/stream`, JWT-via-subprotocol auth + the same ownership-check pattern used everywhere else, decision 13a). Verified against a real debate run with a real WebSocket client (not curl): events (`status_change`, `argument_complete`) arrived live in real time — cross-checked argument IDs against Postgres, all matched; invalid-token and non-owner connection attempts both rejected outright (HTTP 403 at handshake); two simultaneous viewers on the same debate both received an identical event sequence (Redis fan-out, zero connection-manager code needed); connections idle harmlessly after a debate reaches terminal status rather than hanging or erroring. Frontend (removing polling, wiring the WebSocket client) is a separate follow-up. See [docs/adr/0006-redis-websocket-streaming.md](docs/adr/0006-redis-websocket-streaming.md) and [docs/specs/0013-debate-streaming-backend.md](docs/specs/0013-debate-streaming-backend.md). — 2026-07-19

- [x] Round two of UI polish (recalled after using spec 0011's changes): a shared `--panel-shadow` token (styles.css) now gives every card-like container app-wide the floating-panel treatment — login/register cards, debate-thread's header/graph/reading panels, and consultation's chat/picker cards all reference the same value instead of each screen guessing its own. The navbar swapped its flat `border-bottom` for the same shadow (plus a `--ground` background instead of `--page`) so it reads as floating, not integrated. The "Thinking…" bubble now hugs its 3 dots (`width: fit-content`) instead of stretching to a real message's width. The case-type `<select>` got a custom theme-aware arrow (`appearance: none` + a data-URI chevron switched per light/dark theme, matching the existing theme-override pattern already used in styles.css) instead of raw browser chrome. Verified in a real browser via computed-style inspection (not just a glance): border/box-shadow confirmed non-`none` on every panel checked (login, register, two real debate pages, navbar), the pending bubble measured 50px wide against a 414px container (a small pill, not full-width), and the select's `appearance`/`background-image` confirmed as the custom values, not defaults. Zero console errors, zero bugs. See [docs/specs/0012-panel-styling-and-select-fixes.md](docs/specs/0012-panel-styling-and-select-fixes.md). — 2026-07-19

- [x] Consultation chat UI polish + app branding: auto-scroll the message list on new messages/replies (`viewChild` + `afterRenderEffect`), an animated three-dot "Thinking…" indicator (pure CSS), a real navbar with a new Dialex SVG logo (two overlapping divergence/convergence-colored circles — the app's own visual language) used both in the topbar and as the favicon, and a border+shadow on the chat/picker cards so they read as floating panels instead of blending into the page. Verified in a real browser (Canary) across a genuine 5-round OpenAI exchange: auto-scroll confirmed at 0px from bottom after every message with no manual scroll, the typing indicator confirmed as 3 real staggered Web Animations (not static text), tab title/favicon confirmed, navbar branding confirmed, card border/shadow confirmed via computed styles. Zero bugs found — user-reported polish items, not a new feature, and this pass came back clean like the previous milestone. See [docs/specs/0011-consultation-ui-polish-and-branding.md](docs/specs/0011-consultation-ui-polish-and-branding.md). — 2026-07-19

- [x] Consultation frontend chat UI: a case-type picker (`GET /api/case-type-configs/`, new, minimal — just `type`), message thread, and approve button wired to the consultation backend (spec 0009). A real user can now create their own case end-to-end: log in → "New case" → pick a type → negotiate with the AI consultant → approve → land on the real, freshly-created `/debates/:id`. Verified in a real browser (Canary): a genuine multi-turn negotiation (5 real OpenAI round trips, the consultant needed firm repeated prompting before finalizing — its own conversational behavior, not a bug), approve correctly disabled until `ready_to_finalize`, and the resulting debate page rendering the actual discussed case. **Zero app bugs found this pass** — the first milestone this session with a clean verification result; noted honestly rather than manufacturing a "bug found" narrative, since every one of the prior five milestones did find something real. See [docs/specs/0010-consultation-frontend.md](docs/specs/0010-consultation-frontend.md). — 2026-07-19

- [x] Consultant/case-submission stage, backend only: a Temporal `ConsultationWorkflow` (turns delivered via `workflow.update`, not signal+poll or a plain sync bypass) so a real user can negotiate a case with an AI consultant and, on approval, get a real `Case`+`Debate`+`DebateParticipant` set created automatically — no admin involvement. `CaseTypeConfig` gained `default_consultant_persona`/`default_participant_personas`/`default_judge_persona`/`default_max_rounds` to drive that auto-creation; `ConsultationSession.Status` gained `FAILED`. Frontend chat UI is a separate, not-yet-started follow-up milestone. Verified end to end via curl against the real running stack: a multi-turn negotiation correctly accumulating history, approval creating matching real rows, message-after-approval and approve-before-ready both failing cleanly (409/400) rather than hanging or 500ing, IDOR checks clean on both new endpoints, and an integration check chaining the newly-created debate into the existing `/api/debates/{id}/start` endpoint through to a real `Verdict`. Found and fixed three real bugs along the way: two Temporal Activities (`debates` and `consultations`) both named `mark_failed`, which crashes the whole Worker at startup, not just one package (Temporal registers Activities by name across the entire Worker) — renamed the new one to `mark_consultation_failed`; OpenAI's strict structured-output schema mode flatly rejects a free-form `dict` field (verified: it requires every property enumerated with `additionalProperties: false`, incompatible with an open-ended case payload) — fixed by having the LLM emit the payload as a JSON-encoded string, parsed back into a real dict in application code, not the API layer; and two related "retries forever instead of failing fast" gaps — a LangGraph node activity with no bounded `retry_policy` retried a permanent schema error 30+ times before this was even noticed, and a business-validation `ApplicationError` (missing/misconfigured `CaseTypeConfig`) was retryable by default, retrying 3 times before surfacing — both fixed (a bounded `RetryPolicy` on the node, `non_retryable=True` on the validation errors). Also had to unwrap `WorkflowUpdateFailedError`'s nested `.cause` chain correctly in the router — one level deep for an error raised directly in an Update handler, two levels deep for one raised inside an Activity it calls — verified empirically rather than assumed, since getting it wrong silently returned an unhelpful generic message ("Activity task failed") instead of the real one. See [docs/adr/0005-consultation-workflow.md](docs/adr/0005-consultation-workflow.md) and [docs/specs/0009-consultation-workflow-backend.md](docs/specs/0009-consultation-workflow-backend.md). — 2026-07-18

- [x] Wire debate-thread to real orchestration data: new ownership-scoped Django read endpoints (`GET /api/cases/`, `/api/debates/`, `/api/debates/{id}/`, `/api/debates/{id}/arguments/`), a frontend data service replacing debate-thread's mock data, a route param (`/debates/:id`), and a "start debate" button with automatic status polling. Still admin-seeded for test data (no case-submission UI yet). Resolved a real open design gap along the way: a real `Argument.position` (plain string) has no native "how convergent is this" axis value — confirmed with the user to derive it from `CaseTypeConfig.position_options`' list order (now documented as spectrum order, not just valid values), computed server-side, with a distinct-value-clustering fallback for case types with no fixed options. Verified against a real browser (Canary): real data renders correctly (case/status/strategy/round, real argument text, a real verdict), ownership scoping returns a clean not-found (not another user's data or a crash) for both a non-owned debate and a nonexistent one, the bare `/` empty state works. Found and fixed two real bugs: the orchestrator (FastAPI) had no CORS configuration at all, so the frontend's direct calls to it (the start-debate button) failed at the browser's preflight — added `CORSMiddleware`, mirroring Django's existing narrow-allow-list setup; and a polling-race bug where excluding `status='OPEN'` from the polling-start condition meant the page could get stuck showing a stale "OPEN, no arguments" view indefinitely even after the debate had actually run to completion server-side — fixed and re-verified with a gap-free trace (OPEN→ARGUING→NO_CONSENSUS, arguments appearing live, zero manual reloads). See [docs/specs/0008-wire-debate-thread-to-real-data.md](docs/specs/0008-wire-debate-thread-to-real-data.md). — 2026-07-18
- [x] Make debate-thread's view state query-param driven: `mode` (Minimal/Detail) and the selected argument node both read from URL query params on load (falling back to today's defaults if absent/invalid) and written back (via `replaceUrl`, not a new history entry per click) whenever the user changes either — so a specific view state is shareable/bookmarkable/deep-linkable. `theme` stays local (a personal display preference, not "which view of this debate" — a shared link shouldn't force a theme on the recipient). Verified against a real browser (Canary): defaults reflected into the URL on load, node/mode clicks updating the right param without spamming history, a direct navigation to a URL with query params loading straight into that state, invalid params falling back to sane defaults. Also found and fixed a real pre-existing bug this work exposed (not caused by it): `login.ts`/`register.ts`'s post-auth navigation didn't use `replaceUrl`, so a back-press after logging in could re-enter `/login`/`/register`, get bounced by `guestGuard`, and leave a stale-content/bare-URL mismatch on the reused `DebateThread` component. See [docs/specs/0007-debate-thread-query-params.md](docs/specs/0007-debate-thread-query-params.md). — 2026-07-18
- [x] Wire the Angular frontend to real Django auth: `Auth` service (Angular 22's new `@Service()`), functional HTTP interceptor (JWT attach + 401 refresh-retry), route guards, CSRF bootstrap + silent session restore on startup, login/register screens (minimal design pass, reusing existing `--page`/`--ground` tokens). A real user can register/log in/log out through the running app for the first time. Debate-thread stays on mock data — wiring it to real orchestration is a separate follow-up (needs new Django read endpoints that don't exist yet). Verified against a real browser (Canary) across 3 rounds — found and fixed 4 real bugs along the way: Angular's built-in XSRF interceptor silently no-ops cross-origin (had to attach the CSRF header manually), no silent-refresh-on-startup (session didn't survive a reload), `django-cors-headers`' default `CORS_ALLOW_HEADERS` didn't include this project's renamed `x-xsrf-token` header, and `CSRF_TRUSTED_ORIGINS` (a separate check from CORS entirely) had no entry for the dev frontend origin. Also added `django-cors-headers` (new backend dependency, confirmed before adding). See [docs/specs/0006-frontend-auth-wiring.md](docs/specs/0006-frontend-auth-wiring.md) (ADR 0001 already covered the architecture, no new ADR needed). — 2026-07-18
- [x] Build the first real Temporal Workflow (`DebateWorkflow`, sequential turn strategy only) with LangGraph as the per-turn reasoning layer (decisions 2/3), producing a real `Verdict` — plus full OTel observability (trace_id/span_id propagated end to end via Temporal's OTel interceptor, `debate_id`/`session_id`/`user_id` bound explicitly at every Activity) and a `session_id` JWT claim added to already-shipped auth. No research round, no consultant stage, no Redis/WebSocket streaming yet — those are later milestones. Verified end to end against real OpenAI calls: opening statement, both rounds' arguments (including a correct citation-on-position-change per decision 4), convergence checks, and a judge verdict with forced citations all landed correctly; a missing-API-key run correctly produced `Debate.status=FAILED`. Caught and fixed during implementation: an IDOR on the start endpoint (any authenticated user could start any debate — now checks `Case.created_by`), a `pydantic-settings` gap where an empty `OPENAI_API_KEY` silently passed validation, and a wrong assumption that `opentelemetry-instrumentation-logging` would auto-populate trace fields (fixed by reading the OTel span context directly instead). See [docs/adr/0004-temporal-langgraph-orchestration.md](docs/adr/0004-temporal-langgraph-orchestration.md) and [docs/specs/0005-debate-workflow.md](docs/specs/0005-debate-workflow.md). — 2026-07-18
- [x] Scaffold the FastAPI service (skeleton, SQLAlchemy Core DB access, JWT verification, `sqlacodegen` committed for real) — Temporal/LangGraph/WebSocket deferred to a later milestone. See [docs/specs/0004-fastapi-scaffold.md](docs/specs/0004-fastapi-scaffold.md) and [docs/adr/0003-fastapi-architecture.md](docs/adr/0003-fastapi-architecture.md). — 2026-07-18
- [x] Implement Django auth endpoints (register/login/refresh/logout) per decision 13/13a, and smoke-test `sqlacodegen` against the real schema (decision 9). See [docs/specs/0003-auth-endpoints.md](docs/specs/0003-auth-endpoints.md). — 2026-07-18
- [x] Scaffold the Django backend (custom user model, `src/`-layout, core domain models across 6 apps) and set up docker-compose for app Postgres, Temporal (+ its own Postgres + Web UI), and Redis. See [docs/specs/0002-backend-scaffold-and-infra.md](docs/specs/0002-backend-scaffold-and-infra.md) and [docs/adr/0002-backend-architecture.md](docs/adr/0002-backend-architecture.md). — 2026-07-18
- [x] Scaffold the Angular frontend project and build the live-debate "thread" screen as a real, interactive component, driven by mock data (no backend yet) — so the validated design can actually be clicked through in a running app rather than judged from a static preview. See [docs/specs/0001-frontend-scaffold-debate-thread.md](docs/specs/0001-frontend-scaffold-debate-thread.md) and [docs/adr/0001-frontend-architecture.md](docs/adr/0001-frontend-architecture.md). — 2026-07-18
