# TODO

## Open

_(nothing open right now)_

## Done

- [x] Consultation chat UI polish + app branding: auto-scroll the message list on new messages/replies (`viewChild` + `afterRenderEffect`), an animated three-dot "Thinking…" indicator (pure CSS), a real navbar with a new Dialex SVG logo (two overlapping divergence/convergence-colored circles — the app's own visual language) used both in the topbar and as the favicon, and a border+shadow on the chat/picker cards so they read as floating panels instead of blending into the page. Verified in a real browser (Canary) across a genuine 5-round OpenAI exchange: auto-scroll confirmed at 0px from bottom after every message with no manual scroll, the typing indicator confirmed as 3 real staggered Web Animations (not static text), tab title/favicon confirmed, navbar branding confirmed, card border/shadow confirmed via computed styles. Zero bugs found — user-reported polish items, not a new feature, and this pass came back clean like the previous milestone. See [docs/specs/0011-consultation-ui-polish-and-branding.md](docs/specs/0011-consultation-ui-polish-and-branding.md). — 2026-07-19

- [x] Consultation frontend chat UI: a case-type picker (`GET /api/case-type-configs/`, new, minimal — just `type`), message thread, and approve button wired to the consultation backend (spec 0009). A real user can now create their own case end-to-end: log in → "New case" → pick a type → negotiate with the AI consultant → approve → land on the real, freshly-created `/debates/:id`. Verified in a real browser (Canary): a genuine multi-turn negotiation (5 real OpenAI round trips, the consultant needed firm repeated prompting before finalizing — its own conversational behavior, not a bug), approve correctly disabled until `ready_to_finalize`, and the resulting debate page rendering the actual discussed case. **Zero app bugs found this pass** — the first milestone this session with a clean verification result; noted honestly rather than manufacturing a "bug found" narrative, since every one of the prior five milestones did find something real. See [docs/specs/0010-consultation-frontend.md](docs/specs/0010-consultation-frontend.md). — 2026-07-19

- [x] Consultant/case-submission stage, backend only: a Temporal `ConsultationWorkflow` (turns delivered via `workflow.update`, not signal+poll or a plain sync bypass) so a real user can negotiate a case with an AI consultant and, on approval, get a real `Case`+`Debate`+`DebateParticipant` set created automatically — no admin involvement. `CaseTypeConfig` gained `default_consultant_persona`/`default_participant_personas`/`default_judge_persona`/`default_max_rounds` to drive that auto-creation; `ConsultationSession.Status` gained `FAILED`. Frontend chat UI is a separate, not-yet-started follow-up milestone. Verified end to end via curl against the real running stack: a multi-turn negotiation correctly accumulating history, approval creating matching real rows, message-after-approval and approve-before-ready both failing cleanly (409/400) rather than hanging or 500ing, IDOR checks clean on both new endpoints, and an integration check chaining the newly-created debate into the existing `/api/debates/{id}/start` endpoint through to a real `Verdict`. Found and fixed three real bugs along the way: two Temporal Activities (`debates` and `consultations`) both named `mark_failed`, which crashes the whole Worker at startup, not just one package (Temporal registers Activities by name across the entire Worker) — renamed the new one to `mark_consultation_failed`; OpenAI's strict structured-output schema mode flatly rejects a free-form `dict` field (verified: it requires every property enumerated with `additionalProperties: false`, incompatible with an open-ended case payload) — fixed by having the LLM emit the payload as a JSON-encoded string, parsed back into a real dict in application code, not the API layer; and two related "retries forever instead of failing fast" gaps — a LangGraph node activity with no bounded `retry_policy` retried a permanent schema error 30+ times before this was even noticed, and a business-validation `ApplicationError` (missing/misconfigured `CaseTypeConfig`) was retryable by default, retrying 3 times before surfacing — both fixed (a bounded `RetryPolicy` on the node, `non_retryable=True` on the validation errors). Also had to unwrap `WorkflowUpdateFailedError`'s nested `.cause` chain correctly in the router — one level deep for an error raised directly in an Update handler, two levels deep for one raised inside an Activity it calls — verified empirically rather than assumed, since getting it wrong silently returned an unhelpful generic message ("Activity task failed") instead of the real one. See [docs/adr/0005-consultation-workflow.md](docs/adr/0005-consultation-workflow.md) and [docs/specs/0009-consultation-workflow-backend.md](docs/specs/0009-consultation-workflow-backend.md). — 2026-07-18

- [x] Wire debate-thread to real orchestration data: new ownership-scoped Django read endpoints (`GET /api/cases/`, `/api/debates/`, `/api/debates/{id}/`, `/api/debates/{id}/arguments/`), a frontend data service replacing debate-thread's mock data, a route param (`/debates/:id`), and a "start debate" button with automatic status polling. Still admin-seeded for test data (no case-submission UI yet). Resolved a real open design gap along the way: a real `Argument.position` (plain string) has no native "how convergent is this" axis value — confirmed with the user to derive it from `CaseTypeConfig.position_options`' list order (now documented as spectrum order, not just valid values), computed server-side, with a distinct-value-clustering fallback for case types with no fixed options. Verified against a real browser (Canary): real data renders correctly (case/status/strategy/round, real argument text, a real verdict), ownership scoping returns a clean not-found (not another user's data or a crash) for both a non-owned debate and a nonexistent one, the bare `/` empty state works. Found and fixed two real bugs: the orchestrator (FastAPI) had no CORS configuration at all, so the frontend's direct calls to it (the start-debate button) failed at the browser's preflight — added `CORSMiddleware`, mirroring Django's existing narrow-allow-list setup; and a polling-race bug where excluding `status='OPEN'` from the polling-start condition meant the page could get stuck showing a stale "OPEN, no arguments" view indefinitely even after the debate had actually run to completion server-side — fixed and re-verified with a gap-free trace (OPEN→ARGUING→NO_CONSENSUS, arguments appearing live, zero manual reloads). See [docs/specs/0008-wire-debate-thread-to-real-data.md](docs/specs/0008-wire-debate-thread-to-real-data.md). — 2026-07-18
- [x] Make debate-thread's view state query-param driven: `mode` (Minimal/Detail) and the selected argument node both read from URL query params on load (falling back to today's defaults if absent/invalid) and written back (via `replaceUrl`, not a new history entry per click) whenever the user changes either — so a specific view state is shareable/bookmarkable/deep-linkable. `theme` stays local (a personal display preference, not "which view of this debate" — a shared link shouldn't force a theme on the recipient). Verified against a real browser (Canary): defaults reflected into the URL on load, node/mode clicks updating the right param without spamming history, a direct navigation to a URL with query params loading straight into that state, invalid params falling back to sane defaults. Also found and fixed a real pre-existing bug this work exposed (not caused by it): `login.ts`/`register.ts`'s post-auth navigation didn't use `replaceUrl`, so a back-press after logging in could re-enter `/login`/`/register`, get bounced by `guestGuard`, and leave a stale-content/bare-URL mismatch on the reused `DebateThread` component. See [docs/specs/0007-debate-thread-query-params.md](docs/specs/0007-debate-thread-query-params.md). — 2026-07-18
- [x] Wire the Angular frontend to real Django auth: `Auth` service (Angular 22's new `@Service()`), functional HTTP interceptor (JWT attach + 401 refresh-retry), route guards, CSRF bootstrap + silent session restore on startup, login/register screens (minimal design pass, reusing existing `--page`/`--ground` tokens). A real user can register/log in/log out through the running app for the first time. Debate-thread stays on mock data — wiring it to real orchestration is a separate follow-up (needs new Django read endpoints that don't exist yet). Verified against a real browser (Canary) across 3 rounds — found and fixed 4 real bugs along the way: Angular's built-in XSRF interceptor silently no-ops cross-origin (had to attach the CSRF header manually), no silent-refresh-on-startup (session didn't survive a reload), `django-cors-headers`' default `CORS_ALLOW_HEADERS` didn't include this project's renamed `x-xsrf-token` header, and `CSRF_TRUSTED_ORIGINS` (a separate check from CORS entirely) had no entry for the dev frontend origin. Also added `django-cors-headers` (new backend dependency, confirmed before adding). See [docs/specs/0006-frontend-auth-wiring.md](docs/specs/0006-frontend-auth-wiring.md) (ADR 0001 already covered the architecture, no new ADR needed). — 2026-07-18
- [x] Build the first real Temporal Workflow (`DebateWorkflow`, sequential turn strategy only) with LangGraph as the per-turn reasoning layer (decisions 2/3), producing a real `Verdict` — plus full OTel observability (trace_id/span_id propagated end to end via Temporal's OTel interceptor, `debate_id`/`session_id`/`user_id` bound explicitly at every Activity) and a `session_id` JWT claim added to already-shipped auth. No research round, no consultant stage, no Redis/WebSocket streaming yet — those are later milestones. Verified end to end against real OpenAI calls: opening statement, both rounds' arguments (including a correct citation-on-position-change per decision 4), convergence checks, and a judge verdict with forced citations all landed correctly; a missing-API-key run correctly produced `Debate.status=FAILED`. Caught and fixed during implementation: an IDOR on the start endpoint (any authenticated user could start any debate — now checks `Case.created_by`), a `pydantic-settings` gap where an empty `OPENAI_API_KEY` silently passed validation, and a wrong assumption that `opentelemetry-instrumentation-logging` would auto-populate trace fields (fixed by reading the OTel span context directly instead). See [docs/adr/0004-temporal-langgraph-orchestration.md](docs/adr/0004-temporal-langgraph-orchestration.md) and [docs/specs/0005-debate-workflow.md](docs/specs/0005-debate-workflow.md). — 2026-07-18
- [x] Scaffold the FastAPI service (skeleton, SQLAlchemy Core DB access, JWT verification, `sqlacodegen` committed for real) — Temporal/LangGraph/WebSocket deferred to a later milestone. See [docs/specs/0004-fastapi-scaffold.md](docs/specs/0004-fastapi-scaffold.md) and [docs/adr/0003-fastapi-architecture.md](docs/adr/0003-fastapi-architecture.md). — 2026-07-18
- [x] Implement Django auth endpoints (register/login/refresh/logout) per decision 13/13a, and smoke-test `sqlacodegen` against the real schema (decision 9). See [docs/specs/0003-auth-endpoints.md](docs/specs/0003-auth-endpoints.md). — 2026-07-18
- [x] Scaffold the Django backend (custom user model, `src/`-layout, core domain models across 6 apps) and set up docker-compose for app Postgres, Temporal (+ its own Postgres + Web UI), and Redis. See [docs/specs/0002-backend-scaffold-and-infra.md](docs/specs/0002-backend-scaffold-and-infra.md) and [docs/adr/0002-backend-architecture.md](docs/adr/0002-backend-architecture.md). — 2026-07-18
- [x] Scaffold the Angular frontend project and build the live-debate "thread" screen as a real, interactive component, driven by mock data (no backend yet) — so the validated design can actually be clicked through in a running app rather than judged from a static preview. See [docs/specs/0001-frontend-scaffold-debate-thread.md](docs/specs/0001-frontend-scaffold-debate-thread.md) and [docs/adr/0001-frontend-architecture.md](docs/adr/0001-frontend-architecture.md). — 2026-07-18
