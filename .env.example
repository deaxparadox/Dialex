# Copy to .env. docker-compose reads this automatically for ${VAR} substitution.
# App secrets (DJANGO_SECRET_KEY, SIMPLE_JWT_SIGNING_KEY) live in backend/.env
# instead — see backend/.env.example.

POSTGRES_USER=
POSTGRES_PASSWORD=
POSTGRES_DB=
POSTGRES_PORT=5433

TEMPORAL_POSTGRES_USER=
TEMPORAL_POSTGRES_PASSWORD=
TEMPORAL_VERSION=1.29.1
POSTGRESQL_VERSION=16
TEMPORAL_UI_VERSION=2.34.0

REDIS_PORT=6380
