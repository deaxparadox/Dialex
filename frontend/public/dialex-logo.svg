<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 32 32" role="img" aria-label="Dialex">
  <!-- Two overlapping circles in the app's own divergence/convergence
       colors (hardcoded, not CSS vars — a favicon file can't read the
       app's custom properties) — the same visual language already used
       throughout the debate-thread graph, applied to the brand mark. -->
  <circle cx="12" cy="16" r="10" fill="#D9713E" opacity="0.85"/>
  <circle cx="20" cy="16" r="10" fill="#1E9E82" opacity="0.85"/>
</svg>
