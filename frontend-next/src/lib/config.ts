// Ported from frontend/src/environments/environment.ts — same hardcoded
// local-dev constants, no env-var indirection introduced in this phase.
export const config = {
  djangoApiBase: 'http://localhost:8000',
};
