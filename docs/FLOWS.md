# User-facing flows

> Status legend: ✅ done · ⚠️ partial/needs attention · 🚧 in progress · ❌ not started. Kept in sync with reality every commit that touches a flow — see this repo's CLAUDE.md.

| Flow | Status | Notes |
|---|---|---|
| **Register / log in** | ✅ | Full stack done and verified: Django backend (`POST /api/auth/{register,login,refresh,logout}/`, decisions 13/13a) plus real Angular frontend wiring (spec 0006 — `Auth` service, interceptor, route guards, login/register screens). A real user can register, log in, and log out through the running app. Verified against a real browser (Canary), 3 rounds, 4 bugs found and fixed along the way (cross-origin CSRF header handling, session-restore-on-reload, two distinct CORS/CSRF backend gaps) — final round: all 7 checks clean. |
| **Submit a case via consultation** | ✅ | Full stack done and verified (ADR 0005/spec 0009 backend, spec 0010 frontend): a logged-in user clicks "New case," picks a case type, negotiates with a real AI consultant persona (Temporal `ConsultationWorkflow`, turns delivered via `workflow.update` — plain synchronous request/response, no WebSocket needed), and on approval lands on the freshly-created real `/debates/:id`. The consultant now genuinely reflects on its own draft before replying (spec 0023/ADR 0008): a `draft → critique → conditional revise` graph catches cases where the first draft would have accepted something unclear/inconsistent in the user's message at face value — the concrete problem that motivated this, a real session where the consultant parroted back an evident typo three times, is what this closes. A live step indicator (a new SSE connection, additive — the actual reply's request/response contract is unchanged) swaps the generic "Thinking…" label for "Double-checking…"/"Revising…" while those extra steps run, so the added latency doesn't ship silently. Verified end to end in a real browser across 4 consultation turns: correct label transitions (including turns where revision correctly did and didn't trigger), zero console errors, approve/finalize unaffected — real consultation turns via direct API calls also confirmed the revised text (not the discarded draft) is what's actually persisted and shown. Also verified: login → consultation → a genuine multi-turn negotiation → approve → real debate page rendering the actual discussed case. One UX note, not a bug: the consultant can take several back-and-forth turns before finalizing even when told plainly to do so — its own conversational behavior, worth knowing if a tighter turn count is ever expected. |
| **View a live debate** | ✅ | Full stack real and working end-to-end: a logged-in user can open `/debates/:id`, click "Start debate," and watch it run — real Temporal `DebateWorkflow` + LangGraph + OpenAI arguments, convergence checks, and a judge verdict (spec 0005) — via a real WebSocket (`WS /api/debates/{id}/stream`, ADR 0006/specs 0013-0014) pushing `argument_complete`/`status_change` events the instant they happen, in place of the original 4s polling (spec 0008). Polling is kept only as an explicit, logged fallback if the socket drops unexpectedly. The debate itself now renders as a single scrolling chat thread (spec 0016) — bubbles left/right by agent, full text inline, round dividers, an inline "responds to" citation line for rebuttals, and the verdict as the thread's final entry — replacing an earlier scatter-plot node graph (spec 0015) that had accumulated real complexity (lanes, a legend, node animations, a split verdict panel) without the underlying signal (a self-reported confidence float on free text) being precise enough to deserve it; that whole mechanism, including a logged race-condition bug in its live-follow behavior, was removed rather than patched. The judge's opening statement now renders as the thread's first entry once ready, with a loading indicator filling what used to be a silent ~20s dead stretch after clicking "Start debate" that read as frozen (spec 0017). The page now also shows **live "who's currently generating" indicators** (specs 0018/0019) — a new `turn_started` WS event, fired the instant an agent's/judge's LLM call begins (not just completes), drives a "{agent} is thinking…" bubble in the correct position/side for every stage (opening statement, each round's arguments, the verdict), replaced by real content the moment it lands — plus avatar icons, timestamps, and genuinely distinct per-agent bubble colors. This directly answers the original decision 12/spec 0001 ask for a "currently generating" indicator, which had been dropped when token-by-token content itself was ruled out (ADR 0006) — the two turned out to be separable, and only the content part was actually infeasible. **All three turn types now genuinely stream token-by-token** — arguments (spec 0020), then the judge's opening statement and verdict (spec 0021), completing ADR 0007. The earlier belief that Temporal itself blocked this was wrong (an Activity can publish to Redis mid-execution like any side effect) — the real constraint was combining streaming with structured-output judgment calls, resolved by splitting each graph's generation into a plain-streamed content call plus (where the schema actually mixes prose and judgment) a fast follow-up call for judgment fields only; the opening statement's schema was pure prose and needed no follow-up call at all. Verified in a real browser across several real debates: word-by-word visible growth confirmed frame-by-frame for all three stages, no refetch-storm during streaming, clean swap to final content with no reversion to a loading indicator (a real bug found and fixed during spec 0021's verification), no console errors. Known gaps: no "browse my debates" list screen — a direct link to a debate id is the only way in; a bigger, deliberately-deferred redesign question remains open — should agents cite specific structured case facts (and convergence be computed over those) instead of a self-reported confidence float, which none of this changed. |
| Review a judged debate (`HumanReview`) | ❌ | Designed (decision 8), no implementation yet. |
| Browse case/debate history | ❌ | Not designed as a screen yet beyond the PRD's screen inventory. |
| Notifications inbox | ❌ | Designed (decision 17), no implementation yet. |

## How debugging/tracing works, end to end

> Status: **implemented and verified** — [ADR 0004](adr/0004-temporal-langgraph-orchestration.md) / [spec 0005](specs/0005-debate-workflow.md). Confirmed against a real debate run: one shared `trace_id` across the HTTP start call, the Temporal Workflow, and every Activity; `debate_id`/`session_id`/`user_id` present on every log line.

Three separate IDs answer three different debugging questions. They get conflated under "request ID" easily, so here's what each one is actually for and where to find it.

| ID | Answers | Where it comes from | Where it shows up |
|---|---|---|---|
| **`trace_id` / `span_id`** (OpenTelemetry) | "What happened during *this one operation* — which service touched it, in what order, how long did each step take?" | Auto-generated by OTel the moment a traced operation starts (an HTTP request into Django/orchestrator, or a Temporal workflow/activity execution); propagated automatically across service boundaries via `temporalio.contrib.opentelemetry.TracingInterceptor` on the shared Temporal client — no code has to thread it through by hand. | Every span (visible on console output for now — decision 16's "local today, swappable to a real backend later, no code change"); read directly from `opentelemetry.trace.get_current_span()` and stamped onto every log line by our own logging filter (not `opentelemetry-instrumentation-logging` — its record injection turned out to require `set_logging_format=True`, which conflicts with a custom formatter). |
| **`debate_id`** | "Show me everything that happened across this debate's *entire* multi-hour lifecycle" — which spans many separate traces, since a Temporal workflow outlives any single HTTP request/trace. | The real `Debate.id` already in Postgres — no new ID minted. | Attached explicitly as a span attribute (`dialex.debate_id`) and log field at the top of every Activity and the debate-start endpoint — has to be explicit because a Temporal Workflow and its Activities don't share memory, so nothing propagates this implicitly the way `trace_id` does. |
| **`session_id` (+ `user_id`)** | "Which logged-in session/person was behind this" — independent of which debate; useful for auth-flow issues, or "what was this user doing" across multiple debates. | Minted once at login (a UUID, stored as a custom JWT claim) and carried forward through refresh-token rotation — this system uses stateless JWTs (decision 13), not server-side sessions, so there was no session concept to reuse otherwise. | Bound into logs/spans wherever a request carries a JWT — decoded directly by Django and by the orchestrator's `get_auth_context`, no extra lookup needed. |

**Worked example — "debate #482 looks stuck, what happened?"**
1. Filter the rotating log files (or, once swapped to a real backend, the trace UI) for `debate_id=482` — this surfaces every log line across Django, the orchestrator API process, and the Temporal worker for that debate's entire run, regardless of how many separate HTTP calls or Activity retries were involved.
2. Pick the specific round/step that looks wrong and read its `trace_id` — pulling that trace shows the exact causal chain for just that one Activity execution (which LLM call, how long it took, whether it retried) without the noise of the debate's other rounds.
3. If the report came from a user ("my debate seems broken"), their `session_id`/`user_id` (from their auth token) cross-references straight to which debates/cases they've touched, independent of knowing a specific `debate_id` up front.
